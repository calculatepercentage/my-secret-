<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Secret Video Stream</title>
  <style>
    body {
      margin: 0;
      background-color: #111;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
    }

    header {
      background-color: #1f1f1f;
      padding: 15px;
      text-align: center;
      font-size: 1.5em;
      color: #fff;
      box-shadow: 0 2px 5px rgba(0,0,0,0.3);
    }

    .grid {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 25px;
      padding: 20px;
    }

    .card {
      width: 95%;
      max-width: 900px;
      background-color: #000;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 0 15px rgba(255,255,255,0.08);
      transition: transform 0.3s ease;
      position: relative;
    }

    .card:hover {
      transform: scale(1.01);
    }

    .video-wrapper {
      position: relative;
      width: 100%;
      padding-top: 56.25%; /* 16:9 aspect ratio */
    }

    .video-wrapper iframe {
      position: absolute;
      top: 0; left: 0;
      width: 100%;
      height: 100%;
      border: none;
    }

    @media (max-width: 600px) {
      .grid {
        padding: 10px;
        gap: 15px;
      }
    }
  </style>
</head>

<body>

  <!-- Header -->
  <header>Secret Video Stream</header>

  <!-- Video List -->
  <div class="grid" id="videoGrid">Loading videos...</div>

  <script>
    const txtUrl = "https://raw.githubusercontent.com/calculatepercentage/my-secret-/main/your.txt"; 
    const videoGrid = document.getElementById("videoGrid");

    fetch(txtUrl)
      .then(res => res.text())
      .then(text => {
        const codes = text.split("\n").map(c => c.trim()).filter(Boolean);
        if (!codes.length) {
          videoGrid.textContent = "No videos found.";
          return;
        }

        videoGrid.innerHTML = "";
        codes.forEach(code => {
          const card = document.createElement("div");
          card.className = "card";

          const wrapper = document.createElement("div");
          wrapper.className = "video-wrapper";

          const iframe = document.createElement("iframe");
          iframe.src = `https://xhamster.com/embed/${code}`;
          iframe.allowFullscreen = true;

          wrapper.appendChild(iframe);
          card.appendChild(wrapper);
          videoGrid.appendChild(card);
        });
      })
      .catch(err => {
        console.error(err);
        videoGrid.textContent = "Failed to load videos.";
      });
  </script>

</body>
</html>
